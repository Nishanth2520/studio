# This file is intentionally left blank.
# Firebase client-side configuration is managed via environment variables
# as defined in src/lib/firebaseConfig.ts, which should be populated
# from your .env.local file or Vercel environment variables.

# Secrets and API keys should NOT be hardcoded here or in any client-side code.
# See .env.local.example or your .env.local file for where to put these.
